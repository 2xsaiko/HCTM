plugins {
    id 'org.jetbrains.kotlin.jvm'
}

archivesBaseName = 'hctm-base'

dependencies {
    modCompile('therealfarfetchd.qcommon:cfg:1.0.0-8') {
        exclude group: 'net.fabricmc.fabric-api'
    }
}